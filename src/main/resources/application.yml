spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    # execute data.sql
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: philocsera
  h2:
    console:
      enabled: true
jwt:
  issuer: 39philocsera@gmail.com
  secret_key: stargazer